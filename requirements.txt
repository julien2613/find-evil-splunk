splunk-sdk>=3.0.0    # official SDK: ingestion (index.attached_socket) + splunklib.ai agent
# Forensic extraction + ingestion pipeline
volatility3==2.28.0   # process extraction from the memory image
yara-x==1.17.0        # Python bindings (the scan also uses the `yr` CLI)
polars==1.41.2        # fast DataFrames (cross-artifact correlation)
duckdb==1.5.3         # SQL queries on the artifacts
pyarrow==24.0.0       # Parquet I/O
requests==2.34.2      # HEC + MCP JSON-RPC client

# System prerequisites (outside pip):
#   brew install yara-x        -> provides the `yr` binary used by yara_scan.py
#   Splunk Enterprise 9.x/10.x -> local instance (HEC + MCP Server)
