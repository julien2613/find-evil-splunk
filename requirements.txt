# Pipeline d'extraction + ingestion forensique
volatility3==2.28.0   # extraction processus depuis l'image mémoire
yara-x==1.17.0        # bindings Python (le scan utilise aussi le CLI `yr`)
polars==1.41.2        # DataFrames rapides (corrélation cross-artefact)
duckdb==1.5.3         # requêtes SQL sur les artefacts
pyarrow==24.0.0       # I/O Parquet
requests==2.34.2      # HEC + client MCP JSON-RPC

# Prérequis système (hors pip) :
#   brew install yara-x        -> fournit le binaire `yr` utilisé par yara_scan.py
#   Splunk Enterprise 9.x/10.x -> instance locale (HEC + MCP Server)
