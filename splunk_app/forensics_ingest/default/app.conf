[install]
is_configured = 1
state = enabled

[package]
id = forensics_ingest

[ui]
is_visible = 0
label = Find Evil — Forensics TA (index + sourcetypes)

[launcher]
author = Julien Taste
description = Technology Add-on: forensics index + sourcetypes (JSON parsing, _time). Ingestion via splunk-sdk-python (see ingest_to_splunk.py).
version = 1.0.0
