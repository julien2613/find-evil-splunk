[install]
is_configured = 1
state = enabled

[package]
id = forensics_ingest

[ui]
is_visible = 0
label = Find Evil — Forensics TA (index + sourcetypes)

[launcher]
author = Julien Taste
description = Technology Add-on : index forensics + sourcetypes (parsing JSON, _time). Ingestion via splunk-sdk-python (voir ingest_to_splunk.py).
version = 1.0.0
